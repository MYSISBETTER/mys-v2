local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "MYS'S CLIENT V2",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "🔥MYS'S CLIENT V2🔥",
   LoadingSubtitle = "by MYS Motherfucker",
   Theme = "Amethyst", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "MYS_MENU_HUB"
   },

   Discord = {
      Enabled = true, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "https://discord.gg/djMQRCNAAZ", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = True, -- Set this to true to use our key system
   KeySettings = {
      Title = "🔥MYS'S CLIENT V2🔥",
      Subtitle = "KEY IN DISCORD..",
      Note = "Join Server From Misc Tab", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = false, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = true, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"https://pastebin.com/raw/JkXitxep"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local MainTab = Window:CreateTab("🪐HOME ", nil) -- Title, Image
local MainSection = MainTab:CreateSection("- MAIN -")

Rayfield:Notify({
   Title = "You Executed My Script..",
   Content = "Have Fun Fucking Newgens😂 ",
   Duration = 5,
   Image = nil,
})


local Slider = MainTab:CreateSlider({
   Name = "Change Your Fucking Walk Speed😂",
   Range = {0, 999},
   Increment = 1,
   Suffix = "WalkSpeed",
   CurrentValue = 16,
   Flag = "Slider1", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = (Value)
   end,
})

local Button = MainTab:CreateButton({
   Name = "Default WalkSpeed",
   Callback = function()
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = (16)
   end,
})


local SecSection = MainTab:CreateSection("- 🔥 PreMade Scripts 💯 -")

local Button = MainTab:CreateButton({
   Name = "Infinite Yield",
   Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "empty tools",
   Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/likelysmith/EmptyTools/main/script"))()
   end,
})

local Button = MainTab:CreateButton({
   Name = "System Broken",
   Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/H20CalibreYT/SystemBroken/main/script'))()
   end,
})


local Button = MainTab:CreateButton({
   Name = "Emote Script",
   Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Gi7331/scripts/main/Emote.lua"))()
   end,
})


local Button = MainTab:CreateButton({
   Name = "Face Fuck - Press Z when activated",
   Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/saya420/Mic-Up/main/facefuck'))()
   end,
})


local SecTab = Window:CreateTab("- MYS'S SCRIPTS💯 -", nil) -- Title, Image
local MainSection = SecTab:CreateSection("- MYS'S SCRIPTS ARE BETTER MOTHERFUCKER😂 -")


local Button = SecTab:CreateButton({
   Name = "Teleport Side To Side idk how to fucking explain it tf😂",
   Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/MYSISBETTER/mysSTS/refs/heads/main/MYS%20(STS)%20SCRIPT"))()
   end,
})


local Button = SecTab:CreateButton({
   Name = "MYS_BasePlate",
   Callback = function()
   loadstring(game:HttpGet("https://pastebin.com/raw/1DJp5VWq"))()
   end,
})


local Button = SecTab:CreateButton({
   Name = "Rewind - Hold C when activated",
   Callback = function()
   loadstring(game:HttpGet("https://raw.githubusercontent.com/saya420/Mic-Up/main/Flashback-Rewind"))();
   end,
})

local Toggle = SecTab:CreateToggle({
    Name = "Fly",
    CurrentValue = false,
    Flag = "Toggle1", -- Ensure unique flag for configuration saving
    Callback = function(Value)
        local player = game.Players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoidRootPart = character:WaitForChild("HumanoidRootPart")
        local humanoid = character:WaitForChild("Humanoid")
        local flying = Value -- Toggle fly on/off based on the toggle state
        local speed = 50 -- Adjust the fly speed
        local bodyVelocity
        local bodyGyro
        local flyConnection

        -- Function to start flying
        local function startFlying()
            -- Create BodyVelocity to simulate flight
            bodyVelocity = Instance.new("BodyVelocity")
            bodyVelocity.MaxForce = Vector3.new(10000, 10000, 10000)  -- Adjust max force
            bodyVelocity.Velocity = Vector3.zero
            bodyVelocity.Parent = humanoidRootPart

            -- Create BodyGyro to stabilize the player
            bodyGyro = Instance.new("BodyGyro")
            bodyGyro.MaxTorque = Vector3.new(0, 0, 0)  -- Adjust max torque
            bodyGyro.CFrame = humanoidRootPart.CFrame
            bodyGyro.Parent = humanoidRootPart

            -- Update flight controls on every frame
            flyConnection = game:GetService("RunService").Heartbeat:Connect(function()
                local direction = Vector3.zero
                local camera = workspace.CurrentCamera

                -- WASD controls for flight direction
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then
                    direction = direction + camera.CFrame.LookVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then
                    direction = direction - camera.CFrame.LookVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then
                    direction = direction - camera.CFrame.RightVector
                end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then
                    direction = direction + camera.CFrame.RightVector
                end

                -- Apply the velocity and update BodyGyro
                bodyVelocity.Velocity = direction * speed
                bodyGyro.CFrame = CFrame.new(humanoidRootPart.Position, humanoidRootPart.Position + direction)
            end)
        end
        
        -- Function to stop flying
        local function stopFlying()
            if flyConnection then
                flyConnection:Disconnect() -- Disconnect the Heartbeat to stop updating flight controls
                flyConnection = nil
            end

            -- Destroy BodyVelocity and BodyGyro when stopping the fly
            if bodyVelocity then
                bodyVelocity:Destroy()
                bodyVelocity = nil
            end
            if bodyGyro then
                bodyGyro:Destroy()
                bodyGyro = nil
            end
        end

        -- Toggle the fly state
        if flying then
            startFlying()  -- Start flying when toggle is on
        else
            stopFlying()   -- Stop flying when toggle is off
        end
    end
})




local Button = SecTab:CreateButton({
    Name = "Tp Tool",
    Callback = function()
        local player = game.Players.LocalPlayer
        local mouse = player:GetMouse()

        -- Create the teleport tool
        local tool = Instance.new("Tool")
        tool.Name = "Teleport Tool"
        tool.RequiresHandle = false
        tool.Parent = player.Backpack

        -- Function to teleport to mouse click
        local function teleportToClick()
            local character = player.Character or player.CharacterAdded:Wait()
            local rootPart = character:FindFirstChild("HumanoidRootPart")

            if rootPart and mouse.Target then
                local targetPosition = mouse.Hit.Position
                rootPart.CFrame = CFrame.new(targetPosition + Vector3.new(0, 3, 0)) -- Offset to avoid clipping
            end
        end

        -- Connect the tool's activation to the teleport function
        tool.Activated:Connect(teleportToClick)
    end
})



local ThuTab = Window:CreateTab("🌐 Misc ", nil) -- Title, Image
local MainSection = ThuTab:CreateSection("- Misc -")

-- Create Label in the correct section
local Label = MainSection:CreateLabel("Copy Discord Link Below", Color3.fromRGB(255, 255, 255), false) -- Corrected to MainSection

-- Create Button in the same section
local Button = MainSection:CreateButton({
    Name = "Copy Discord Link",
    Callback = function()
        setclipboard("https://discord.gg/djMQRCNAAZ") -- Copies the link to the clipboard
        print("Discord link copied to clipboard!")

        -- Change button text after clicking
        Button:Set("Name", "Copied to clipboard!") -- Updates the button label (if supported)
    end
})
